server:
  port: 8080

spring:
  application:
    name: langgraph-ui
  jackson:
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    dev.langgraph: DEBUG
    org.springframework.web.socket: DEBUG
